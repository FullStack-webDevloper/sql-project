<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Data</title>
    <style>
        table,
        th,td{
            border: 1px solid black;
        }
    </style>
</head>
<body>
    <table>
        <tr>
            <th>Id</th>
            <th>Username</th>
            <th>Email</th>
        </tr>
        <%for(usersdata of users){%>
         <tr>
            <td><%=usersdata.id%></td>
            <td><%=usersdata.username%></td>
            <td><%=usersdata.email%></td>
            <td>
               <form method="GET" action="/users/<%= usersdata.id%>/edit">
                    <button>Edit</button>
                </form>
            </td>
            <td>
                <form method="post" action="/users/<%= usersdata.id%>?_method=DELETE">
                    <button>Delete</button>
                </form>
            </td>
         </tr>
        <%}%>
    </table>
</body>
</html>